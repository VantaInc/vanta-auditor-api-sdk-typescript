{
  "name": "vanta-auditor-api-sdk-smoke-test",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Live smoke test against a real Vanta API tenant. See ../SMOKE_TEST.md.",
  "scripts": {
    "test:module-format": "npm --prefix ../module-format-test test",
    "test:live": "vitest run --reporter=default --reporter=junit --outputFile.junit=test-report.xml",
    "test": "npm run test:module-format && npm run test:live"
  },
  "dependencies": {
    "vanta-auditor-api-sdk": "file:.."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "~5.8.3",
    "vitest": "^4.1.6"
  }
}
