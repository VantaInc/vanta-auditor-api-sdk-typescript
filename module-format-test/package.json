{
  "name": "vanta-auditor-api-sdk-module-format-test",
  "version": "1.0.0",
  "private": true,
  "description": "Offline ESM + CJS import checks for dual packaging. See ../SMOKE_TEST.md.",
  "scripts": {
    "test": "npm run test:esm && npm run test:cjs",
    "test:esm": "npm --prefix esm install && npm --prefix esm test",
    "test:cjs": "npm --prefix cjs install && npm --prefix cjs test"
  }
}
